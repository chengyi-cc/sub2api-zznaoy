# 官方上游 v0.2.8 合并复核

日期：2026-09-23。本次范围为本地代码合并及推送，不创建发布标签或部署服务器。

## 基线与范围

- 合并前：`a56e68e6f`，已完整撤销 13 次主动打票相关提交。
- 官方上游：`a3eb7ef30`，版本号 `0.2.8`。
- 共同基线：`98d86915b`。
- 合并前恢复分支：`backup/before-upstream-sync-20260923-220816`。
- 原打票版本另有完整源码及提交历史备份，见项目旁的 `sub2api-new-backup-before-remove-turn-state-20260923-215836` 目录。
- 对比共同基线，本地自有差异涉及 315 个文件，上游修改涉及 863 个文件，重叠 96 个文件，明确冲突 15 个文件。复核同时比较合并前后的自有差异内容，没有仅依靠合并工具的冲突标记。

## 冲突处理与兼容性

| 接点 | 合并结果 |
| --- | --- |
| 自定义菜单 | 保留位置、内嵌/新标签打开方式；加入上游“隐藏打开按钮”设置。 |
| 发票与公开设置 | 保留发票功能、最低开票金额和管理入口；加入上游订阅开关。 |
| 请求诊断 | 同时保留客户端状态码、上游状态码、总耗时和首字响应时间；查询列、扫描顺序及桌面/手机显示保持对应。 |
| 模型提示词 | 保留本地模型别名归一化与固定版本提示词，接入上游非 GPT 模型身份声明清理以及新模型识别。 |
| 长连接池 | 保留账号身份及 TLS（加密连接参数）配置隔离，接入上游断连回收与空闲连接保活。 |
| 服务装配 | 重新运行 Wire（生成服务依赖连接代码的工具），保留发票服务与加密连接配置，接入上游插件账号目录和用量服务。 |
| 版本 | 采用上游 `0.2.8`；没有创建或推送发布标签。 |

发票、异步图片任务、图片按次计费、强制优先处理、自定义首页及俄语适配的自有差异仍然保留。主动打票、打票代理池、无票换号、打票管理页面和相应接口没有重新引入。正常协议的回合状态头转发仍保留。

## 合并后的修复

- 更新双方请求诊断测试的数据列，增加状态码与首字响应时间共存的断言；新增桌面和手机布局的组合回归测试。
- 上游新增页面测试补齐语言状态，匹配本地多语言数字及时间格式化逻辑。
- 非 GPT 提示词测试按照实际路由模型选择基准；没有 GPT 身份声明的固定版本回退提示词要求逐字保留，仍验证输出不含 GPT 身份声明。
- 冷却回调测试明确设置不同的重置时间，避免连续读取系统时间相同而没有真正构造“旧回调”场景。
- 代码规范检查排除已经生成的首页预览压缩文件，继续检查源代码；没有关闭规则或跳过业务测试。

## 验证与限制

- 前端：340 个测试文件、2,615 项测试全部通过；生产构建、类型检查、语言键检查及完整代码规范检查通过。
- 后端编译及 `go vet`（Go 静态检查）通过。
- 后端完整单元测试 `go test -tags=unit -timeout=10m ./...` 与集成测试 `go test -tags=integration -timeout=10m ./...` 最终均通过；未跳过失败项目。首次并发运行出现过验证码传输错误测试失败，重跑整个数据访问包及最终全量均通过，未改动该测试或业务实现。
- 包含前端资源的 Linux amd64（服务器常用处理器架构）构建 `go build -tags=embed -trimpath ./cmd/server` 通过；这是交叉编译验证，没有启动 Linux 服务。
- 生成的服务依赖代码已重新生成；没有打票功能引用或未解决的合并标记。
- 容器配置安全、网关环境变量、运行资源及反向代理缓存/流式配置检查通过。
- macOS 专用权限测试在 Windows 无法通过；需要容器工具和 Unix 命令环境的简易模式配置、发布脚本测试未在本机完成验证，不计为通过。没有执行真实容器发布。
- 原始日志与前端结构化测试报告存放于本地 `.cache/upstream-sync-20260923/`，不进入版本库。

## 上游带来的实际数据变更

本次没有连接或修改线上数据库。未来部署 `0.2.8` 时，上游迁移会清理三档限额均为空的平台配额记录、扩展推理强度计费倍率配置、增加联盟提现幂等标识等。这些属于上游新增行为，不能把本地测试通过理解为已经完成生产数据迁移验证。

本地原有未提交的首页图片保留在工作区，未混入合并提交。

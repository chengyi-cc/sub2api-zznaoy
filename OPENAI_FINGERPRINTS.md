# OpenAI 账号指纹功能

## 核对依据与范围

参考目录是 Sub2API-R7-Deploy（他人二开的部署包），清单声明不包含对应源码。以下行为通过本地二进制函数及调用路径核对后，在 sub2api-new（本项目源码）中重建；不是原作者源码的逐行移植，也不是向真实上游做过的黑盒对照。

只同步单机多窗口和 OpenAI OAuth（登录授权账号）的 TLS（加密连接握手）指纹功能，不改其他功能，不连接生产数据库，不自动部署或重启。

## 单机多窗口

管理界面创建 OpenAI OAuth 账号时默认选择 machine（单机多窗口），编辑与批量编辑也支持该档位。已有账号缺省仍按 off（关闭本层收敛）处理，已有显式档位保持不变；通过接口创建账号需显式提交所需档位。**不移植参考包强制修改旧账号默认值的迁移。**

### 标识规则

- 设备标识收敛到账号设备身份；只改写已经存在且非空的字段，不为普通客户端补造设备、会话、窗口信息。
- 会话、线程、父线程和缓存键使用 HMAC-SHA256（带密钥的稳定摘要）映射：密钥是账号持久化种子，输入为 `sub2api:codex-machine:v1:` 加去掉首尾空白的原始标识。
- 与参考包一致，不再额外混入下游 API Key（用户接入密钥）编号；同一账号、同一原始标识，即使来自不同接入密钥也得到相同假名。不同原始标识映射为不同假名，不改写对话内容。
- 输入是 UUIDv7（带时间信息的唯一标识）时保留其时间前缀及版本；其他输入映射为 UUIDv4（另一种唯一标识格式）。同一请求或长连接内重复处理不会再次散列已生成的假名。
- 窗口标识只有在主体可解析为 UUID（标准唯一标识）时才改写；冒号后缀原样保留，包括前导零、非数字后缀和空后缀。不认识的窗口格式保持原样。
- 请求头始终移除 conversation_id（旧会话头）；仅当存在 session-id 或 thread-id（新式会话或线程头）时移除 session_id（旧式会话头）。不删除请求体里的会话字段。
- 回合元数据里的 turn_id（单轮请求标识）与时间不重新生成；父线程、派生线程一并映射。重复顶层字段仅保留最后一个，未知字段和大整数不会被浮点转换破坏。
- sandbox（运行环境标记）按账号/网关解析得到的规范出站 User-Agent（客户端身份说明）中的系统统一：Mac 对应 seatbelt，Windows 对应 windows_sandbox，其他已识别系统对应 seccomp；这三者是不同系统的隔离机制标签。仅替换已知标签，Windows 的 windows_elevated（提权环境）保留；不补造缺失字段，也不改未知值。
- 有效 machine 模式跳过旧账号命名空间的预先改写，避免原始标识被重复映射；缺失有效种子时仍走旧隔离规则。关闭、重新开启保留原种子。

### 已接入路径

| 路径 | 行为 |
| --- | --- |
| 普通请求与透传 | 请求头、请求体、缓存键共享同一份映射 |
| compact（上下文压缩）请求 | machine 生效；其他旧档位保持原有压缩处理 |
| 后台压缩探测 | 生成完整探测身份与压缩元数据，再统一映射请求头和请求体；请求长度及重试读体同步更新 |
| WebSocket（双向长连接）直接入口 | 连接池、透传的首帧与后续帧均处理；握手头使用相同映射；切换账号时清除旧上下文 |
| Chat Completions（聊天兼容接口）与 Messages（消息兼容接口） | 协议转换后暂存映射并处理请求体；最终发送前再次清理兼容分支生成的旧会话头 |

## TLS 指纹模拟

创建和编辑 OpenAI OAuth 账号可开启 TLS 指纹模拟并选择模板。界面默认关闭，配置键为 enable_tls_fingerprint（是否启用）与 tls_fingerprint_profile_id（模板编号）；未指定模板使用内置默认，-1 表示随机模板，关闭时清除模板绑定。

常规 HTTP（请求/响应协议）转发与账号测试读取同一模板服务。**插件优先接管请求；WebSocket 握手不由此开关修改，这与参考包的传输调用路径一致，并非本次漏接。** 长连接的业务身份字段仍受上面的 machine 模式控制，两者不是同一个功能。

普通 API Key 类型上游账号不启用此模拟，原有 Claude 账号支持不变。内置模板是 Node.js 24.x（JavaScript 运行环境）的握手特征，不是经过验证的 Codex 官方指纹。不承诺降低封禁概率、增加额度或提高缓存命中率。

## 验证

- 固定摘要向量、UUIDv7 时间保留、窗口后缀、缺失字段、重复字段、大整数、系统标签以及并发重复映射测试。
- 普通与透传请求、压缩请求、后台压缩探测、聊天与消息兼容入口的本地请求捕获测试。
- 本地 WebSocket 客户端连接真实入口、模拟上游传输，验证连接池与透传模式各两轮请求，以及握手头与请求体一致性。
- 旧指纹档位、账号隔离、压缩、长连接入口、兼容接口及插件相关回归测试；服务入口编译检查与静态检查。单机映射及两种长连接路径的竞态检测（检查并发访问冲突）通过。
- 前端创建、编辑、批量编辑账号及语言键完整性测试；类型与格式检查。

部署前仍应在测试环境用实际代理链路验证。上述测试不等于生产效果或真实网络握手的抓包验证。
